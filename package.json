{
  "name": "typerogue",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "TypeRogue — a typing-first roguelike light-gun shooter in the browser",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.0",
    "vite": "^5.4.0"
  }
}
